Resume Builder ERP — Frontend scaffold

This folder contains a lightweight scaffold for an ERP-style frontend built with React + Vite.

To run:

1. cd erp
2. npm install
3. npm run dev

Notes:
- The scaffold uses simple in-memory/localStorage mocks for APIs.
- Use this as a starting point for building a full ERP frontend.
